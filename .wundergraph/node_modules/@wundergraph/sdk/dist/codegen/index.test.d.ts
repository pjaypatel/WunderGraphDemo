import { Template } from "./index";
export interface EvaluateTemplate {
    equals: (expected: {
        [key: string]: string;
    }) => void;
}
export declare const RunTemplateTest: (...templates: Template[]) => Promise<EvaluateTemplate>;
