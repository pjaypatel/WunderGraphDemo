"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.federationServiceSDL = exports.isFederationService = void 0;
const axios = require('axios').default;
const isFederationService = (schema) => {
    const queryType = schema.getQueryType();
    if (queryType === undefined || queryType === null) {
        return false;
    }
    const fields = queryType.getFields();
    if (fields === undefined) {
        return false;
    }
    return Object.keys(fields).indexOf("_service") !== -1;
};
exports.isFederationService = isFederationService;
const federationServiceSDL = async (url) => {
    const data = JSON.stringify({
        query: '{_service{sdl}}'
    });
    const res = await axios.post(url, data, {
        headers: {
            "Content-Type": "application/json",
            "Accept": "application/json",
        }
    });
    return res.data.data._service.sdl;
};
exports.federationServiceSDL = federationServiceSDL;
