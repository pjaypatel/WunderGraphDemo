"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const renametypes_1 = require("./renametypes");
const graphql_1 = require("graphql");
const input = `
type Query {
    me: User!
}
interface Node {
    id: ID!
}
type User implements Node {
    id: ID!
    name: String!
}
union Entities = User
`;
const expected = `
type Query {
  me: PetStore_User!
}
interface PetStore_Node {
    id: ID!
}
type PetStore_User implements PetStore_Node {
    id: ID!
    name: String!
}
union PetStore_Entities = PetStore_User
`;
test("renameTypes", () => {
    const prettyExpected = graphql_1.print(graphql_1.parse(expected));
    const actual = renametypes_1.renameTypes(input, [
        {
            from: "User",
            to: "PetStore_User"
        }, {
            from: "Node",
            to: "PetStore_Node"
        }, {
            from: "Entities",
            to: "PetStore_Entities"
        }
    ]);
    chai_1.assert.equal(actual, prettyExpected);
});
