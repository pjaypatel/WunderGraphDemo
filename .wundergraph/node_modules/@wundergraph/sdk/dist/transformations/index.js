"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const renameTypes = async (api, ...rename) => {
    const resolved = await api;
    resolved.renameTypes(rename);
    return resolved;
};
const renameFields = async (api, ...rename) => {
    const resolved = await api;
    resolved.renameTypeFields(rename);
    return resolved;
};
const transformApi = {
    renameTypes,
    renameFields,
};
exports.default = transformApi;
