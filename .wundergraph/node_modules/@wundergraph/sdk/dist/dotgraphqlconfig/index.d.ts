import { WunderGraphConfigApplicationConfig } from "../configure";
export interface DotGraphQLConfigOptions {
    baseURL: string;
}
export declare const generateDotGraphQLConfig: (wunderGraphConfig: WunderGraphConfigApplicationConfig, options: DotGraphQLConfigOptions) => Config;
interface Config {
    projects: Projects;
}
interface Projects {
    [project: string]: Project;
}
interface Project {
    name: string;
    schemaPath: string;
    extensions: Extensions;
}
interface Extensions {
    endpoints: Endpoints;
}
interface Endpoints {
    [name: string]: Endpoint;
}
interface Endpoint {
    url: string;
    headers: Headers;
    introspect: boolean;
}
interface Headers {
    [key: string]: string;
}
export {};
