"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stringToSnakeCase = exports.stringToCamelCase = void 0;
const stringToCamelCase = (str) => {
    if (str.indexOf("_") === -1) {
        return str;
    }
    return str.toLowerCase().replace(/([-_][a-z])/g, group => group
        .toUpperCase()
        .replace('-', '')
        .replace('_', ''));
};
exports.stringToCamelCase = stringToCamelCase;
const stringToSnakeCase = (str) => {
    const out = str.replace(/[A-Z]/g, letter => `_${letter.toLowerCase()}`);
    if (out.indexOf("_") === 0) {
        return out.slice(1);
    }
    return out;
};
exports.stringToSnakeCase = stringToSnakeCase;
